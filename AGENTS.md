# 穩盈價值雷達（WenYing Value Radar）開發與進版規範 (Agent Guidelines & Memory)

本文件記載專案核心開發規範與進版標準，所有後續開發與發布作業必須嚴格遵守。

---

## 1. 進版發布標準 SOP (Version Revision & Testing Sync)

每次進行功能更新、邏輯優化或介面修改後，必須完成以下同步：

1. **更新頁尾版本號與變更紀錄**：
   - 檔案：`app/site-footer.tsx`
   - 更新 `SITE_REVISION = "Rev. YYYY.MM.DD.N"`。
   - 在 `REVISION_ENTRIES` 最上方新增當前版本的**中英文對照詳細變更紀錄**。
2. **關鍵：同步更新 HTML 渲染測試的預期版本字串**：
   - 檔案：`tests/rendered-html.test.mjs`
   - 必須將測試中的版本正則表達式（約在第 51 行）同步更新為相同版本號：
     ```javascript
     assert.match(html, /Rev\. YYYY\.MM\.DD\.N/);
     ```
3. **完成編譯與全套測試**：
   - 執行 `npx vinext build` 確保編譯成功。
   - 執行 `node --experimental-strip-types --test tests/*.test.mjs` 確保所有測試 100% 通過（目前共 206+ 項測試）。
4. **提交並推播**：
   - `git add -A ; git commit -m "..." ; git push origin main`

---

## 2. 技術分析形態量化標準 (Technical Analysis Quant Rules)

### A. 早晨之星／黃昏之星 (Morning Star / Evening Star)
- **前期連續波段**：前 5~15 個交易日必須為連續明確之下跌波段（跌幅 >= 4~15%，晨星）或上漲波段（夜星），嚴禁盤整橫盤跳空（排除 1537 廣隆）。
- **首根長實體出量**：第一根為長陰／長陽實體，伴隨恐慌拋售或追價爆量（>= 0.85x MA20 Volume）。
- **第二根十字星創波段新極值**：星線最低點必須為近 15~25 根 K 線的最低點（**左側不可有更低的 K 線**，排除突破回測頸線之 2354 鴻準），且必須精確落在日/週/月支撐線上。
- **第三根實體收復 >= 50%**：確認型態要求第三根長陽收復首根實體 50% 以上。

### B. 順勢交易（W底 / 均線收攏買點）(Trend Pullback Entry)
- **均線金叉強制約束**：15EMA 必須嚴格位於 50SMA 之上（`15EMA >= 50SMA`，黃金交叉），死叉空頭排列（`15EMA < 50SMA`）一律 100% 嚴格排除（排除 2385 群光、8299 群聯）。
- **第一階段（推升波）**：日線歷經下跌築底後，出現顯著推升波（漲幅 >= 10%~25%），帶動 15EMA 與 50SMA 黃金交叉並向上打開（Peak Spread >= 3.5%）。
- **第二階段（高檔回調）**：高檔箱體橫盤回調，第 1 次回調打出初期低點時均線開口尚未完全收攏。
- **第三階段（第二次/第三次回踩收攏買點）**：必須已完成**第二次（或第三次）回踩前次同一固定水平支撐低點**，且 15EMA 與 50SMA 完全收攏黏合（差幅 <= 3.5%），50MA 走平或向上支撐，此處為最高勝率之順勢起漲買點。
- **排除未完成回踩標的**：若股價衝高後懸在半空、尚未回踩前次低點（如 8299 目前價格 2210 遠高於前次低點 1950），絕不列入觀察。

### C. 量化真實性原則 (Quant Integrity)
- 若市場當日無任何標的嚴格符合條件，**回傳空陣列 [] 並顯示專業 Empty State 說明卡片，絕不硬湊或偽造不合格個股**。

---

## 3. UI 與資料慣例
- **漲跌顏色慣例**：台股遵循紅漲、綠跌（`.theme-red-up`）。
- **即時公允價值聯動**：技術分析與排行榜標的點擊皆可直達公允價值詳細頁面。
